# Requirements for train_hybrid-mamba-bitnet.py
# Optimized for NVIDIA RTX 4070 Ti Super (16GB) + Ryzen 9 7950X

# Core
torch>=2.0.0
numpy>=1.24.0

# Tensor operations
einops>=0.7.0

# Build dependencies (required for mamba-ssm installation)
wheel
setuptools
ninja
packaging

# Mamba SSM optimized CUDA kernels (requires CUDA toolkit)
causal-conv1d>=1.4.0
mamba-ssm>=1.0.0

# Experiment tracking (optional, but recommended)
wandb>=0.15.0
